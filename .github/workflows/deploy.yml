name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # ou la branche que vous voulez déployer

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Ajout des permissions d'écriture
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js 📦
        uses: actions/setup-node@v3
        with:
          node-version: '20' # ou la version que vous utilisez

      - name: Install dependencies ⚙️
        run: npm install

      - name: Build 🏗️
        run: npm run build

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages # La branche où le build sera déployé
          folder: dist # Le dossier contenant les fichiers buildés
